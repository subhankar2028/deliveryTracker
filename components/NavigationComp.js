import React, { Component } from 'react';
import { View, Text } from 'react-native';

class NavigationComp extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> NavigationComp </Text>
      </View>
    );
  }
}

export default NavigationComp;
